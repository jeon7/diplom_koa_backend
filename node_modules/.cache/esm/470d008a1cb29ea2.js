let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_1cb‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_1cb‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_1cb‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_1cb‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_1cb‍.w("./api/index",[["default",["api"],function(v){api=v}]]);_1cb‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







// import createFakeData from './createFakeData'; // to test pagination

const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true
  })
  .then(() => {
    console.log('Connected to MongoDB');
    // createFakeData(); // to test pagination
  })
  .catch(e => {
    _1cb‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes());
app.use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  console.log(`Listening to port ${port}`);
});
